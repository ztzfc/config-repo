zuul:
#全部服务忽略敏感头(全部服务都可以传递cookie)
  sensitive-headers:
  routes:
    aaaa:
      path: /myProduct/**
      serviceId: product
    #简洁写法
#    product: /myProduct/**
  ignored-patterns:
    - /**/product/listForOder
